<h1>WareHouse App</h1>

<h2>Produtos Cadastrados</h2>
<% @product_models.each do |p| %>
  <p><%= link_to(p.name, product_model_path(p.id))%>
  <%= p.sku %><%= 'inativo' %></p>
  <%= button_to 'ativar', activate_product_models_path(p.id), {method:'patch'} %>
<% end%>

